diff -urpN a/cipher.h b/cipher.h
--- a/cipher.h	2015-07-20 13:15:51.854561081 -0500
+++ b/cipher.h	2015-07-17 14:57:41.309968448 -0500
@@ -62,6 +62,12 @@
 #define CIPHER_ENCRYPT		1
 #define CIPHER_DECRYPT		0
 
+#define SECURE_CIPHERS 	"chacha20-poly1305@openssh.com,"\
+						"blowfish-cbc,"\
+						"aes256-gcm@openssh.com,"\
+						"aes128-gcm@openssh.com,"\
+						"aes256-ctr,aes192-ctr,aes128-ctr"
+
 struct sshcipher;
 struct sshcipher_ctx {
 	int	plaintext;
diff -urpN a/servconf.c b/servconf.c
--- a/servconf.c	2015-07-20 13:15:43.438654926 -0500
+++ b/servconf.c	2015-07-17 14:57:48.197887398 -0500
@@ -331,6 +331,8 @@ fill_default_server_options(ServerOption
 		options->ip_qos_interactive = IPTOS_LOWDELAY;
 	if (options->ip_qos_bulk == -1)
 		options->ip_qos_bulk = IPTOS_THROUGHPUT;
+	if (options->ciphers == NULL)
+		options->ciphers = xstrdup(SECURE_CIPHERS);
 	if (options->version_addendum == NULL)
 		options->version_addendum = xstrdup("");
 	if (options->fwd_opts.streamlocal_bind_mask == (mode_t)-1)
