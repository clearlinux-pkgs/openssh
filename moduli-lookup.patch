Index: openssh-6.7p1/pathnames.h
===================================================================
--- openssh-6.7p1.orig/pathnames.h	2013-12-07 00:24:02.000000000 +0000
+++ openssh-6.7p1/pathnames.h	2015-03-03 18:00:00.079257287 +0000
@@ -44,6 +44,8 @@
 #define _PATH_HOST_ED25519_KEY_FILE	SSHDIR "/ssh_host_ed25519_key"
 #define _PATH_HOST_RSA_KEY_FILE		SSHDIR "/ssh_host_rsa_key"
 #define _PATH_DH_MODULI			SSHDIR "/moduli"
+/* Stateless moduli fallback */
+#define _PATH_DH_MODULI_STATELESS      	"/usr/share/defaults/ssh/moduli"
 
 #ifndef _PATH_SSH_PROGRAM
 #define _PATH_SSH_PROGRAM		"/usr/bin/ssh"
--- openssh-7.3p1/dh.c~	2016-07-27 22:54:27.000000000 +0000
+++ openssh-7.3p1/dh.c	2016-08-02 14:45:52.942239949 +0000
@@ -152,7 +152,7 @@
 	int linenum;
 	struct dhgroup dhg;
 
-	if ((f = fopen(_PATH_DH_MODULI, "r")) == NULL) {
+	if ((f = fopen(_PATH_DH_MODULI, "r")) == NULL && (f = fopen(_PATH_DH_MODULI_STATELESS, "r")) == NULL) {
 		logit("WARNING: could not open %s (%s), using fixed modulus",
 		    _PATH_DH_MODULI, strerror(errno));
 		return (dh_new_group_fallback(max));
