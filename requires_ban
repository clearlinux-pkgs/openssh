# This file contains runtime requirements that get picked up but are
# undesirable. One entry per line, no whitespace.

# openssh-services only required by sshd which is packaged as
# openssh-extras-server
openssh-services
